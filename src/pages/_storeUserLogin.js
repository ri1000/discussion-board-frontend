import {writable} from 'svelte/store'

export const logedUserRefresh = writable({});
export const logedUsername = writable({});
export const logedUserDetails = writable({});
export const paramsStore = writable({});

